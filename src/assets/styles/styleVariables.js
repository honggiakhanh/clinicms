import { deepPurple } from '@material-ui/core/colors';

const styleVariables = {
  primaryColor300: deepPurple[300],
  primaryColor500: deepPurple[500],
  primaryColor700: deepPurple[700],
  primaryColor800: deepPurple[800],
  textColor: '#828585',
  transitionEffect: 'all 0.2s cubic-bezier(0.645, 0.045, 0.355, 1)',
};

export default styleVariables;
