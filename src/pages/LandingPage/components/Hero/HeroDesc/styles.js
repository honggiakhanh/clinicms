import { makeStyles } from '@material-ui/core';

export default makeStyles({
  heroDescBase: {
    fontSize: "1.25rem",
    fontWeight: 500,
    color: '#828282',
    lineHeight: 1.6,
    letterSpacing: "2px"
  },
  heroDescContent: {
    maxWidth: 500,
    marginBottom: 50,
  },
  heroDescPhone: {
    marginBottom: 20,
  },
});
